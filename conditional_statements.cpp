#include <iostream>
using namespace std;
int main()
{
  /*Conditional statements affect the natural flow of a program and by adding conditions it directs it in a certain way.
  Let us understand it with practical examples.
  */
  int a =  4;
  if (a==4)
  {
    //This block of code executes only if a == 4. Thus, conditional statements.
    cout<<"a is equal to 4.";
  }
  // We can use logical operators to set conditions. Like,
  if (a > 2)
    //print something
  
  

  return 0;

}
