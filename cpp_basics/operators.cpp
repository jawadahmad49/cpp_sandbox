#include<iostream>
using namespace std;
int main()
{
  //There are multiple operators in C++
  //Arithmetic Operators are + , - , *, / and % which calculates the remainder.
  // You can use them directly in expressions Like,
  int a = 8;
  int b = 4;
  //Let's use all of them in a single line with an output statement.
  // Try it right now in any code editor & see the output yourself;
  cout<<a+b<<" " << a - b << " " <<a*b<<" "<<a/b<<" " <<a%b;
  
  
  
 return 0; 
}
